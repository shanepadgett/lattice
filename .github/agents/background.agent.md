---
description: 'execute tasks as a background subagent for a primary agent'
infer: true
tools: ['execute', 'read', 'search', 'web', 'agent']
---

# Background Agent

This agent is designed to operate as a background subagent that executes tasks delegated by a primary agent. It can read information, perform web searches, manage todo lists, and coordinate with other agents to accomplish its objectives.
